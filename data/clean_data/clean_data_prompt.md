说明：我在CLaudeCode里完成了数据清洗任务，所以我把输入Markdown文本之类的地方手动换成了“修改xxx.md文件”之类的描述



## 通用 System Prompt（建议在每次对话都带上）

**System Prompt:**

你是一个严谨的计算机科学教材编辑专家。你的任务是按照用户的特定要求对输入的 Markdown 文本进行清洗和改写。

**严格遵守以下原则：**

1. **绝对不要**改变文本中原有的代码块（python ... ）和数学公式（$...$ 或 $...$）的内容。
2. 仅针对用户要求的特定目标进行修改，其他不相关的部分（标题层级、专业术语、代码注释等）必须原封不动地保留。
3. 直接输出修改后的 Markdown 文本，不要在开头或结尾添加任何多余的解释性废话（如“好的，这是修改后的文本”）。



## 阶段任务prompt：



**Step 1: 删除实验环境申请与登录任务**
目标：去除无关的云平台注册、SSH登录等非知识性操作说明。

**User Prompt:**

请帮我清洗 Markdown 文本。

**清洗要求：**

1. 识别并删除所有关于“申请实验环境”、“登录云平台”、“使用 SSH 登录”等纯平台操作层面的段落和代码块（例如 `ssh root@...` 等）。
2. 保留关于“代码实现”、“运行实验”等直接与 Python 脚本运行相关的核心命令（例如 `python main_exp_3_3.py`）。
保证删除后的文本上下文衔接自然，不留空白标题。

以下是待清洗文本：
[插入你的文本]



**Step 2: 评分标准改写为“推荐实践内容”**
目标：把期末打分的要求，变成对客观技术实践的知识陈述，去除“分数”概念。

**User Prompt:**
请帮我清洗以下 Markdown 文本中的“实验评估”或“打分标准”部分。
清洗要求：

1. 将标题（如存在）中的“实验评估”或类似表述修改为“推荐实践内容”。
2. 彻底删除“60分标准”、“80分标准”、“100分标准”等带有考核性质的词汇。
3. 将原先标准中的要求，改写为陈述性的实践目标。 示例参考：将“100分标准：对使用四重循环计算卷积层和池化层的实现方式进行改进，提升计算速度...” 改写为：“进阶实践：对使用四重循环计算卷积层和池化层的实现方式进行改进，利用矩阵运算提升计算速度...”。

以下是待清洗文本：
[插入你的文本]



**Step 3: 公式引用替换（消除递归依赖）**
目标： 文本在任何位置被截断时，都不会出现孤立的"公式(xxx)"式引用。

**User Prompt：**

原理： 目前文本中存在大量"公式(3.10)"、"式(3.12)"等引用。若文本从此处被截断，读者无法知道 (3.10)是什么公式。必须将每个引用替换为自包含的描述，消除对上游公式块的序号查找依赖。

清洗要求：
1. 找出段落文本中所有形如 公式(3.10)、式(3.12)、根据公式3.10 的序号引用。
2. 对每一处引用，做以下之一的替换（按优先级）：
- 优先1 — inline LaTeX： 若公式较短，替换为公式名 + inline LaTeX，如 "根据内容损失的计算公式(3.10)" → "根据内容损失计算公式
$L_{content} = \frac{1}{2NCHW} \sum (\boldsymbol{X}^l - \boldsymbol{Y}^l)^2$" 。注意此处的公式引用可能和实际的不一样，要以文本实际为主
- 优先2 — 公式名 + 关键变量： 若公式过长（多行公式、Adam 更新组），提取公式名和核心变量，如 "根据公式(3.17)计算梯度的一阶矩和二阶矩" → "根据Adam 优化器的更新公式计算梯度的一阶矩 $m_t$ 和二阶矩 $v_t$"
- 优先3 — 自然语言概括作用： 仅在公式来自外部章节（如公式(3.5)、(3.6)来自第2章，当前文件无对应公式）时，用文字描述其功能，如"根据公式(3.5)和(3.6)，可以进行卷积层反向传播的计算" → "进行卷积层反向传播的计算时，首先计算权重和偏置的梯度..."
3. 保留原本独立成行的公式块（$$...$$）不变，仅修改段落文字中的引用。

禁止： 不允许简单地删除序号而不补充公式内容。删除序号的下一步必须是补入 inline LaTeX
或公式名。只剩"上式"这类指代也算违规——"上式"截断后同样不明确。

以下是待清洗文本：
[插入你的文本]



**Step 4: 去除图表引用**
目标：彻底剥离图表依赖(文本来源的教材pdf中有图标、图片信息，现要完全转化为纯文本)，让文本具备纯粹的“盲读”可理解性。

**User Prompt:**
请帮我清洗 Markdown 文本，消除所有对图片和表格的视觉依赖。文中有多数图片应该只是为了读者方便理解，引用文字周围的上下文会有具体解释图片描述的文字；还有很多单纯的表示这里有一张图片、表示图片名字的“小标题”。你要充分理解语义做出判断。

清洗要求：

1. 目标是删除、修改所有类似“如图3.3所示”、“见图3.4”、“如封面所示”、“如下表所示”的指代图片的引用文字。
2. 如果包含引用的句子仅仅是为了引出图片（例如“初始化后的生成图像如图3.6所示。”），请直接删除整句。
3. 如果句子中不仅包含图片引用，还包含客观的知识描述（例如“初始化后的生成图像如图3.6所示，由于是在内容图像上加入高斯噪声所得，该图像有些模糊。”），请剔除引用部分，保留知识描述（修改为：“初始化后的生成图像是在内容图像上加入高斯噪声所得，该图像有些模糊。”）。
4. 删除纯粹作为图片说明的单独图注（例如“图 3.4 目标风格图像示例”）。

以下是待清洗文本：
[插入你的文本]


**Step 5: 代码引用消除与逻辑内联（专门应对代码块引用）**

**User Prompt:**
请帮我深度清洗 Markdown 文本，彻底消除文中所有对代码块的引用（如“如代码示例3.12所示”、“见代码3.13”等），使得文本在切片后依然具备完整的上下文逻辑。同时在对应代码块中注入注释，保证代码块匹配质量。

【核心原则：三级分类处理】
请仔细阅读含有代码引用的句子、其上下文，以及对应的真实代码块。然后根据以下三种情况，对引用文字进行相应的替换、删除或改写：

*情况 1：上下文已有详细的逻辑描述*

判断标准：文本在引用代码前后，已经详细列出了代码的作用或实现步骤（例如：“数据加载模块的实现如代码示例3.12所示，具体函数功能为：数据加载... 生成图像初始化...”）。

操作：**直接删除**“如代码示例xx所示”这类引用语句，并对上下文进行润色，使文义通畅。

*情况 2：代码仅为补充举例，非核心讨论对象*

判断标准：上下文缺乏详细描述，且该处代码只是一个常规操作的补充说明（例如不涉及底层算法，只是展示如何调用某个库），不影响对核心知识点的理解。

操作：**直接删除**引用语句，使文义通畅。

*情况 3：代码是核心讨论内容，且上下文缺乏逻辑描述*

判断标准：文本直接讨论代码逻辑但没有展开说明（例如：“卷积层的反向传播计算如代码示例3.13所示。然后将扩充的边缘裁剪掉...”），必须知道代码内容才能懂这段话。

操作：**用精炼的自然语言或核心伪代码**替换该引用语句。请阅读对应的代码块，提取其最核心的算法逻辑（如具体的函数名、核心运算的循环结构、关键的 API 调用），填补到原文中。

修改示例：将“卷积层的反向传播计算如代码示例3.13所示”替换为“在卷积层的反向传播中，通过四重循环遍历样本、通道和特征图尺寸，分别累加计算权重、偏置的梯度以及损失函数对输入的偏导”。

【注释注入规则】
在不改变任何原有 Python 代码逻辑的前提下，为文中的`python`代码块添加高密度的自然语言注释：

1. **函数/类级全局说明**：在每一个核心类或函数定义的正上方，添加一段说明，包含“所属实验名称”、“核心任务”和“对应原理”等，可以比较凝练像tag，但是**不要过于凝练**，可以的情况下尽量**使用代码块内注释，或者文本其他部分对代码描述的内容**。 例：# 非实时图像风格迁移中计算内容损失的反向传播。
2. **核心逻辑注释**：在拥有复杂的业务代码（尤其是多层循环、矩阵运算、原填空补全处）的函数内，可以在上述全局说明后另起一行添加描述其具体物理/数学意义的注释，把公式的变量名和代码挂钩。
3. **禁止删除**：增加注释tag不要删除原先在代码块中出现的任何注释，函数内部任何文本无须修改

**【绝对禁忌】**

替换情况 3 时，严禁把几十行原始代码作为普通文本塞进段落里，**必须是自然语言总结或伪代码逻辑**。

以下是待清洗文本：
[插入你的文本]



**Step 6: 实体显式化（解决指代不明与流程重构）**
目标：消除代词，将长段落流程转为清晰的带主语的步骤列表，*极其关键的一步！*

User Prompt:
请帮我深度优化 Markdown 文本的逻辑结构与指代表达，使其适合 RAG（检索增强生成）系统的切分与检索。

清洗要求：

1. **消除代词，显式化实体**：将文本中所有的“本实验”、“该模型”、“这一层”、“此时”等模糊指代，替换为具体的专有名词。例如将“本实验使用 Adam 算法”替换为“在非实时图像风格迁移中，使用 Adam 算法”。
2. **重构长流程文本**：寻找文本中描述“实验流程”、“计算过程”的长段落（通常含有“首先...其次...然后...”）。将这些长段落拆解为带有数字编号的列表（1. 2. 3.）。
3. **强调上下文**：在拆解出的每一个列表步骤开头，必须带上整体任务的上下文。例如不要只写“1. 计算特征图”，而是写“1. 在非实时风格迁移的特征提取阶段，计算特征图”。
4. 保持代码块内部和数学公式原样不变。
   
以下是待清洗文本：
[插入你的文本]



