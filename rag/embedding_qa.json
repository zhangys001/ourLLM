[
    {
        "npyID": "0",
        "Question": "在 CPU 平台上使用四重循环计算卷积层前向传播和反向传播的速度较慢，如何利用高效的矩阵运算库，将四重循环中卷积核与特征图的内积运算（即向量运算）转化为矩阵运算，从而减少循环次数，加速卷积层的运算速度？",
        "Answer": "通过 `im2col` (Image to Column) 算法可将卷积操作转化为通用矩阵乘法 (GEMM)。\n前向传播：将输入特征图的每个感受野（局部窗口）展平为列向量，按顺序拼接成矩阵 $X_{col}$。将卷积核展平为矩阵 $W_{row}$。通过矩阵乘法 $Y_{col} = W_{row} \\times X_{col}$ 计算，再将 $Y_{col}$ 通过 `col2im` 重塑为输出特征图。\n反向传播：同理利用 `im2col` 处理梯度矩阵，将反向卷积转化为输入梯度 $\\nabla X$ 与权重梯度 $\\nabla W$ 的矩阵乘法。\n此方法将四重循环转化为高度优化的 BLAS 库矩阵运算，极大提升内存局部性与计算速度。"
    },
    {
        "npyID": "1",
        "Question": "统计训练过程中每次迭代时每层的前向传播和反向传播时间及其在每层的时间占比，哪些层的时间占比较高？前向传播和反向传播的计算瓶颈在哪些层？",
        "Answer": "在 VGG19 模型的风格迁移中，时间占比最高且构成计算瓶颈的层是卷积层。\n前向传播计算瓶颈：卷积层包含庞大的通道数（如 512）与较大的特征图尺寸，感受野滑动产生的乘加运算量（FLOPs）极大，主导了前向时间。\n反向传播计算瓶颈：由于网络参数固定，无需更新卷积核参数，但仍需计算特征图的梯度 $\\nabla X$ 以一直反传至生成图像。该反向卷积运算的复杂度等同于前向传播。相比之下，激活层仅进行阈值判断，池化层仅记录或反传最大值位置，计算耗时可忽略。因此，卷积层的海量浮点运算是整个流程的核心瓶颈。"
    },
    {
        "npyID": "2",
        "Question": "在第3.1节和第3.2节的实验评估中，均使用给定输入值并与正确的输出值进行比较的方式来确定某个层的实现是否正确。如果正确的输出值无法获知，如何从梯度定义的角度检查层的实现是否正确？（可参考由梯度定义引申出的梯度的数值近似实现方法）",
        "Answer": "通过梯度检验（Gradient Checking）利用有限差分法验证解析梯度的正确性。\n根据泰勒展开式，损失函数 $L$ 对任一变量 $x$ 的导数可由中心差分近似：$\\frac{\\partial L}{\\partial x} \\approx \\frac{L(x + \\epsilon) - L(x - \\epsilon)}{2\\epsilon}$，其中 $\\epsilon$ 为极小值（如 $10^{-4}$）。\n检验步骤：1) 对待测模块分别输入 $x + \\epsilon$ 和 $x - \\epsilon$ 计算两次 Loss；2) 通过差分公式求得数值梯度；3) 调用待测模块的反向传播函数求得解析梯度；4) 计算二者的相对误差：$\\frac{|grad_{num} - grad_{analytic}|}{\\max(|grad_{num}|, |grad_{analytic}|)}$。若相对误差极小（如 $<10^{-5}$），即可判定反向传播实现正确。"
    },
    {
        "npyID": "3",
        "Question": "风格迁移的结果通常是由人直接进行判断，这是一种主观的定性判断方式，不同的人判断结果可能会有较大偏差。如何设计合理的定量判断方法，可以较为客观的评价风格迁移结果的优劣？",
        "Answer": "客观定量评价应围绕\"内容保留度\"与\"风格匹配度\"设计计算指标：\n1. 内容保留度：计算生成图像与原始内容图像在预训练网络（如 VGG）深层特征图上的均方误差 (MSE) 或结构相似度 (SSIM)。误差越小表明语义结构保留越好。\n2. 风格匹配度：计算生成图像与目标风格图像在网络各层特征图上的 Gram 矩阵距离（即风格损失值）。距离越小，说明色彩分布和局部纹理越一致。\n3. 图像平滑度：引入总变分损失 (Total Variation Loss)，计算生成图像相邻像素的绝对差值总和，以评估并惩罚高频异常噪点。\n将以上多维度指标标准化后进行加权统计，即可得到客观定量的评价结果。"
    },
    {
        "npyID": "4",
        "Question": "在实现神经网络基本单元时，如何确保一个网络层的实现是正确的？",
        "Answer": "确保网络层实现正确可通过两种标准验证：\n1. **前向传播验证**：给定特定输入矩阵和层参数（权重、偏置），比对代码计算的输出矩阵与手工/理论公式推导的输出矩阵是否一致。\n2. **反向传播验证**：给定相同的输入矩阵、参数以及模拟的\"损失函数对输出的偏导（即反向传播输入）\"，比对代码计算出的反向传播输出和参数梯度（如 $\\nabla_W L$ 和 $\\nabla_b L$）是否与理论公式结果一致。本实验的60分标准正是要求全连接层、ReLU层和Softmax层在给定输入下，前向与反向的各项输出/梯度结果必须完全正确。"
    },
    {
        "npyID": "5",
        "Question": "在实现神经网络后，如何在不改变网络结构的条件下提高精度？",
        "Answer": "在网络结构（即拓扑结构和各层维度）不变的条件下，可通过调整**训练超参数**来提高精度（本实验90分标准）。具体可调参数包括：\n1. **学习率（$\\eta$）**：调整更新步长，过大易震荡，过小收敛慢。\n2. **批量大小（Batch Size）**：权衡梯度下降的随机性与准确性。\n3. **迭代次数/训练周期（Epochs）**：增加训练次数可让模型更充分学习，但需防止过拟合。\n4. **数据预处理**：确保训练数据充分打乱（Shuffle），防止模型记忆输入顺序，提升泛化能力。"
    },
    {
        "npyID": "6",
        "Question": "如何通过修改网络结构提高精度？可以从哪些方面修改网络结构？",
        "Answer": "通过修改网络结构（本实验100分标准）提高精度，可从以下方面切入：\n1. **调整神经元数量（宽度）**：增加隐层（如 `hidden1`, `hidden2`）的神经元个数，提升单层特征提取和表达能力。\n2. **增加网络层数（深度）**：追加全连接层和对应的激活函数层，使网络能学习更抽象、复杂的非线性特征。但需注意防范梯度消失。\n3. **引入或更换层级结构**：若跳出纯全连接框架，可引入空间变换网络（STN）处理几何变换，或引入残差结构（如 ResNet 中的捷径连接）解决深层网络的退化和梯度消失问题，也可利用批量归一化（BatchNorm）加速收敛并防止剧烈变化。"
    }
]
